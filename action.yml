---
name: Semantic Version Github Action
description: Github Action to adjust version information in antora.yml depending on current branch or version tag.
inputs:
  version:
    description: The version to validate
    required: true
runs:
  using: 'docker'
  image: 'src/main/action/Dockerfile'
  args:
    - ${{ inputs.version }}
